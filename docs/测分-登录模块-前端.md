# 登录模块 — 前端测分文档

**版本**：1.0 | **日期**：2026-07-14 | **作者**：—

---

## 修订记录

| 日期 | 版本 | 修订说明 | 作者 |
|------|------|----------|------|
| 2026-07-14 | 1.0 | 初稿 | — |

---

## 一、功能性测试用例

### 1.1 登录页

#### FE-LOGIN-001: 正常登录

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-001 |
| **用例名称** | 正确账号密码登录成功 |
| **前置条件** | 用户账号存在，密码正确 |
| **操作步骤** | 1. 访问 `/login` → 2. 输入正确账号 → 3. 输入正确密码 → 4. 点击「登录」 |
| **预期结果** | 调 POST `/api/user/login` 成功 → Store 存储 LoginUserVO → 跳转首页 `/` → 左侧菜单按角色正确渲染 |
| **测试目标** | 登录基本流程 |
| **优先级** | P0 |

#### FE-LOGIN-002: 错误密码登录

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-002 |
| **用例名称** | 错误密码提示并清空密码框 |
| **前置条件** | 用户账号存在 |
| **操作步骤** | 1. 输入正确账号 → 2. 输入错误密码 → 3. 点击登录 |
| **预期结果** | 停留在登录页，表单上方红色 Alert "账号或密码错误，请重试"；密码输入框清空；账号输入框保留 |
| **测试目标** | 登录异常处理 |
| **优先级** | P0 |

#### FE-LOGIN-003: 空表单校验

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-003 |
| **用例名称** | 不输入直接点登录显示校验提示 |
| **前置条件** | 访问登录页 |
| **操作步骤** | 1. 不输入任何内容 → 2. 点击登录 |
| **预期结果** | 账号输入框下方 "请输入账号"，密码输入框下方 "请输入密码"；不调 API |
| **测试目标** | 前端表单校验 |
| **优先级** | P0 |

#### FE-LOGIN-004: 只输入账号

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-004 |
| **用例名称** | 仅输入账号不输入密码 |
| **前置条件** | 访问登录页 |
| **操作步骤** | 1. 输入账号 → 2. 不输入密码 → 3. 点击登录 |
| **预期结果** | 密码输入框下方 "请输入密码"；不调 API |
| **优先级** | P1 |

#### FE-LOGIN-005: 登录按钮 Loading 状态

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-005 |
| **用例名称** | 登录中按钮不可重复点击 |
| **前置条件** | 模拟慢网络 |
| **操作步骤** | 1. 输入账号密码 → 2. 快速连续点击 3 次登录 |
| **预期结果** | 第 1 次点击后按钮 loading 转圈 + 置灰；第 2、3 次点击无效；只发了 1 次 API 请求 |
| **测试目标** | 防重复提交 |
| **优先级** | P1 |

#### FE-LOGIN-006: 回车键提交

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-006 |
| **用例名称** | 在密码框按回车触发登录 |
| **前置条件** | 已输入账号和密码 |
| **操作步骤** | 1. 输入账号 → 2. 输入密码 → 3. 按 Enter 键 |
| **预期结果** | 触发登录（与点击登录按钮效果一致） |
| **测试目标** | 键盘交互 |
| **优先级** | P2 |

#### FE-LOGIN-007: 密码可见切换

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-007 |
| **用例名称** | 点击眼睛图标切换密码可见性 |
| **前置条件** | 已输入密码 |
| **操作步骤** | 1. 输入密码 → 2. 点击密码框右侧眼睛图标 |
| **预期结果** | 密码变为明文显示 → 再次点击恢复密文（●●●●） |
| **测试目标** | UI 交互 |
| **优先级** | P2 |

#### FE-LOGIN-008: 记住我功能

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-008 |
| **用例名称** | 勾选记住我后下次自动填充账号 |
| **前置条件** | 首次登录 |
| **操作步骤** | 1. 勾选「记住我」→ 2. 登录成功 → 3. 退出登录 → 4. 再次访问 `/login` |
| **预期结果** | 步骤 4 账号输入框自动填充上次的账号 |
| **测试目标** | 记住我 |
| **优先级** | P2 |

#### FE-LOGIN-009: 网络错误提示

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-009 |
| **用例名称** | 网络断开时登录给出友好提示 |
| **前置条件** | 断网或后端未启动 |
| **操作步骤** | 1. 输入账号密码 → 2. 点击登录 |
| **预期结果** | toast "网络连接失败，请检查网络后重试"；不停在白屏 |
| **测试目标** | 网络异常处理 |
| **优先级** | P1 |

#### FE-LOGIN-010: 已登录访问登录页

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGIN-010 |
| **用例名称** | 已登录用户访问 /login 自动跳转首页 |
| **前置条件** | 已登录 |
| **操作步骤** | 1. 浏览器地址栏输入 `/login` |
| **预期结果** | 路由守卫检测到已登录 → 自动跳转首页 `/` |
| **测试目标** | 路由守卫 |
| **优先级** | P1 |

---

### 1.2 注册页

#### FE-REG-001: 正常注册

| 项目 | 内容 |
|------|------|
| **编号** | FE-REG-001 |
| **用例名称** | 正常注册成功后跳转登录 |
| **前置条件** | 账号未被注册 |
| **操作步骤** | 1. 访问 `/register` → 2. 输入新账号 → 3. 输入密码 → 4. 再次输入密码 → 5. 点击注册 |
| **预期结果** | 调 POST `/api/user/register` 成功 → toast "注册成功，请登录" → 自动跳转 `/login` |
| **测试目标** | 注册基本流程 |
| **优先级** | P0 |

#### FE-REG-002: 密码不一致

| 项目 | 内容 |
|------|------|
| **编号** | FE-REG-002 |
| **用例名称** | 两次密码不一致前端校验 |
| **前置条件** | 访问注册页 |
| **操作步骤** | 1. 输入账号 → 2. 输入密码 "12345678" → 3. 输入确认密码 "87654321" → 4. 点注册 |
| **预期结果** | 确认密码输入框下方 "两次输入的密码不一致"；不调 API |
| **测试目标** | 前端校验 |
| **优先级** | P0 |

#### FE-REG-003: 密码长度不足

| 项目 | 内容 |
|------|------|
| **编号** | FE-REG-003 |
| **用例名称** | 密码少于 8 位被前端拦截 |
| **前置条件** | 访问注册页 |
| **操作步骤** | 1. 输入密码 "123" → 2. 点注册 |
| **预期结果** | 密码输入框下方 "密码至少 8 位"；不调 API |
| **测试目标** | 密码长度校验 |
| **优先级** | P1 |

#### FE-REG-004: 账号长度不足

| 项目 | 内容 |
|------|------|
| **编号** | FE-REG-004 |
| **用例名称** | 账号少于 4 位被前端拦截 |
| **前置条件** | 访问注册页 |
| **操作步骤** | 1. 输入账号 "ab" → 2. 点注册 |
| **预期结果** | 账号输入框下方 "账号至少 4 位"；不调 API |
| **测试目标** | 账号长度校验 |
| **优先级** | P1 |

#### FE-REG-005: 账号已存在

| 项目 | 内容 |
|------|------|
| **编号** | FE-REG-005 |
| **用例名称** | 重复注册提示账号已存在 |
| **前置条件** | 账号已被注册 |
| **操作步骤** | 1. 输入已存在的账号 → 2. 输入密码 → 3. 点击注册 |
| **预期结果** | 后端返回 400 "账号重复" → toast "该账号已被注册" |
| **测试目标** | 后端校验 |
| **优先级** | P1 |

#### FE-REG-006: 已登录访问注册页

| 项目 | 内容 |
|------|------|
| **编号** | FE-REG-006 |
| **用例名称** | 已登录用户访问 /register 自动跳转 |
| **前置条件** | 已登录 |
| **操作步骤** | 1. 地址栏输入 `/register` |
| **预期结果** | 路由守卫检测到已登录 → 跳转首页 |
| **测试目标** | 路由守卫 |
| **优先级** | P1 |

---

### 1.3 路由守卫与权限

#### FE-GUARD-001: 未登录拦截

| 项目 | 内容 |
|------|------|
| **编号** | FE-GUARD-001 |
| **用例名称** | 未登录直接访问受保护页面被拦截 |
| **前置条件** | 未登录 |
| **操作步骤** | 1. 浏览器地址栏输入 `/salary/account` |
| **预期结果** | 路由守卫拦截 → 跳转 `/login?redirect=/salary/account` |
| **测试目标** | 路由守卫-未登录 |
| **优先级** | P0 |

#### FE-GUARD-002: 登录后跳回原页面

| 项目 | 内容 |
|------|------|
| **编号** | FE-GUARD-002 |
| **用例名称** | 从拦截页登录后跳回原目标页 |
| **前置条件** | 未登录 |
| **操作步骤** | 1. 访问 `/salary/account` → 2. 被跳转到 `/login?redirect=/salary/account` → 3. 完成登录 |
| **预期结果** | 登录成功后跳转到 `/salary/account`（而非首页） |
| **测试目标** | redirect 参数 |
| **优先级** | P1 |

#### FE-GUARD-003: 无权限角色拦截

| 项目 | 内容 |
|------|------|
| **编号** | FE-GUARD-003 |
| **用例名称** | employee 访问管理员页面被拦截 |
| **前置条件** | employee 角色已登录 |
| **操作步骤** | 1. 地址栏输入 `/system/user`（需 sys_admin） |
| **预期结果** | 路由守卫检测 role 不匹配 → 跳转 `/403` 页面 |
| **测试目标** | 路由守卫-角色校验 |
| **优先级** | P0 |

#### FE-GUARD-004: 不同角色看到 403 页面

| 项目 | 内容 |
|------|------|
| **编号** | FE-GUARD-004 |
| **用例名称** | 403 页面展示友好提示 |
| **前置条件** | 无权限角色登录 |
| **操作步骤** | 1. 访问无权限的页面 |
| **预期结果** | 展示 403 页面："抱歉，您没有访问此页面的权限" + "返回首页"按钮 |
| **测试目标** | 403 页面 |
| **优先级** | P1 |

---

### 1.4 菜单与 UI 权限

#### FE-MENU-001: 管理员看到全部菜单

| 项目 | 内容 |
|------|------|
| **编号** | FE-MENU-001 |
| **用例名称** | sys_admin 看到所有菜单项 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 查看左侧菜单栏 |
| **预期结果** | 包含：工作台、员工管理、组织架构、考勤管理、薪资管理、我的工资条、审批管理、系统设置、用户管理 |
| **测试目标** | 菜单权限 |
| **优先级** | P0 |

#### FE-MENU-002: HR 看不到系统设置

| 项目 | 内容 |
|------|------|
| **编号** | FE-MENU-002 |
| **用例名称** | hr 角色看不到管理员专属菜单 |
| **前置条件** | hr 已登录 |
| **操作步骤** | 1. 查看左侧菜单栏 |
| **预期结果** | 不包含「系统设置」和「用户管理」 |
| **测试目标** | 菜单动态渲染 |
| **优先级** | P1 |

#### FE-MENU-003: 普通员工只能看到有限菜单

| 项目 | 内容 |
|------|------|
| **编号** | FE-MENU-003 |
| **用例名称** | employee 只能看到基础菜单 |
| **前置条件** | employee 已登录 |
| **操作步骤** | 1. 查看左侧菜单栏 |
| **预期结果** | 只包含：工作台、我的工资条；不包含员工管理、薪资管理、系统设置等 |
| **测试目标** | 菜单动态渲染 |
| **优先级** | P0 |

#### FE-MENU-004: 财务专员菜单

| 项目 | 内容 |
|------|------|
| **编号** | FE-MENU-004 |
| **用例名称** | finance 看到薪资相关菜单 |
| **前置条件** | finance 已登录 |
| **操作步骤** | 1. 查看左侧菜单栏 |
| **预期结果** | 包含：工作台、薪资管理、我的工资条；不包含：员工管理、系统设置 |
| **测试目标** | 菜单动态渲染 |
| **优先级** | P1 |

---

### 1.5 权限指令

#### FE-DIR-001: v-permission:role 隐藏按钮

| 项目 | 内容 |
|------|------|
| **编号** | FE-DIR-001 |
| **用例名称** | 角色不匹配按钮不渲染 |
| **前置条件** | employee 已登录 |
| **操作步骤** | 1. 访问一个页面上有 `v-permission:role="['sys_admin', 'hr']"` 的按钮 |
| **预期结果** | 该按钮 DOM 不存在 |
| **测试目标** | 角色指令 |
| **优先级** | P0 |

#### FE-DIR-002: v-permission:code 隐藏按钮

| 项目 | 内容 |
|------|------|
| **编号** | FE-DIR-002 |
| **用例名称** | 权限码不匹配按钮不渲染 |
| **前置条件** | hr 已登录（无 user:delete 权限） |
| **操作步骤** | 1. 访问有 `v-permission:code="['user:delete']"` 的按钮 |
| **预期结果** | 该按钮 DOM 不存在 |
| **测试目标** | 权限码指令 |
| **优先级** | P1 |

#### FE-DIR-003: v-permission:code.disable 禁用模式

| 项目 | 内容 |
|------|------|
| **编号** | FE-DIR-003 |
| **用例名称** | 禁用模式下按钮显示但不可点击 |
| **前置条件** | hr 已登录 |
| **操作步骤** | 1. 查看有 `.disable` 修饰符的按钮 |
| **预期结果** | 按钮存在但 disabled + 灰色样式 + hover 显示 tooltip "您没有此操作的权限" |
| **测试目标** | 禁用模式 |
| **优先级** | P2 |

---

### 1.6 退出登录

#### FE-LOGOUT-001: 正常退出

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGOUT-001 |
| **用例名称** | 退出登录清除状态并跳转 |
| **前置条件** | 已登录 |
| **操作步骤** | 1. 点击顶部用户头像 → 2. 点击「退出登录」 |
| **预期结果** | 调 POST `/api/user/logout` → Store 中 userInfo 清空 → 跳转 `/login` → 再访问任何页都被拦截 |
| **测试目标** | 退出登录 |
| **优先级** | P0 |

#### FE-LOGOUT-002: 退出后浏览器后退

| 项目 | 内容 |
|------|------|
| **编号** | FE-LOGOUT-002 |
| **用例名称** | 退出后点浏览器后退不回退到已登录页 |
| **前置条件** | 已登录后退出 |
| **操作步骤** | 1. 退出登录 → 2. 点浏览器后退按钮 |
| **预期结果** | 不回到已登录页面（路由守卫拦截 → 跳转登录页） |
| **测试目标** | 安全性 |
| **优先级** | P1 |

---

### 1.7 Session 过期处理

#### FE-SESS-001: Session 过期后自动跳转

| 项目 | 内容 |
|------|------|
| **编号** | FE-SESS-001 |
| **用例名称** | Session 过期后任意操作跳转登录 |
| **前置条件** | 已登录，Session 超时 |
| **操作步骤** | 1. 等待 Session 过期 → 2. 点击任意菜单或刷新页面 |
| **预期结果** | 后端返回 401 → Axios 拦截器捕获 → Store 清空 → 跳转 `/login` + toast "登录已过期，请重新登录" |
| **测试目标** | Axios 拦截器 |
| **优先级** | P0 |

#### FE-SESS-002: 页面刷新恢复登录态

| 项目 | 内容 |
|------|------|
| **编号** | FE-SESS-002 |
| **用例名称** | 刷新页面后正常恢复用户信息 |
| **前置条件** | 已登录 |
| **操作步骤** | 1. F5 刷新页面 |
| **预期结果** | App 挂载时调 GET `/api/user/get/login` → 成功 → Store 恢复 userInfo → 页面正常渲染 |
| **测试目标** | 登录态恢复 |
| **优先级** | P0 |

#### FE-SESS-003: 页面刷新恢复失败

| 项目 | 内容 |
|------|------|
| **编号** | FE-SESS-003 |
| **用例名称** | Session 已过期时刷新跳转登录 |
| **前置条件** | 曾登录过但 Session 已过期 |
| **操作步骤** | 1. 等待 Session 过期 → 2. F5 刷新 |
| **预期结果** | GET `/api/user/get/login` 返回 401 → 跳转 `/login` |
| **测试目标** | 登录态恢复失败 |
| **优先级** | P1 |

---

### 1.8 用户管理页（管理员）

#### FE-USER-001: 用户列表加载

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-001 |
| **用例名称** | 管理员查看用户列表 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 进入 `/system/user` |
| **预期结果** | 表格渲染，字段正确（ID/账号/昵称/角色/简介/创建时间），分页组件显示 |
| **测试目标** | 用户管理列表 |
| **优先级** | P1 |

#### FE-USER-002: 新增用户

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-002 |
| **用例名称** | 管理员创建新用户 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 点"新增用户" → 2. 填账号+昵称+选角色 → 3. 点保存 |
| **预期结果** | Modal 关闭 → 列表刷新出现新用户（默认密码 12345678 由后端生成） |
| **测试目标** | 新增用户 |
| **优先级** | P1 |

#### FE-USER-003: 编辑用户角色

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-003 |
| **用例名称** | 管理员修改用户角色 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 点某行的"编辑" → 2. 改角色为 "hr" → 3. 保存 |
| **预期结果** | 调 POST `/api/user/update` → 列表刷新 → 该行角色 Tag 变为 "HR专员" |
| **测试目标** | 编辑用户 |
| **优先级** | P1 |

#### FE-USER-004: 删除用户

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-004 |
| **用例名称** | 管理员删除用户 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 点某行的"删除" → 2. 确认弹窗 → 3. 点确定 |
| **预期结果** | 调 POST `/api/user/delete` → 列表刷新该记录消失 |
| **测试目标** | 删除用户 |
| **优先级** | P1 |

#### FE-USER-005: 删除取消

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-005 |
| **用例名称** | 删除确认取消 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 点"删除" → 2. 弹确认 → 3. 点取消 |
| **预期结果** | Modal 关闭，数据不变 |
| **测试目标** | 二次确认 |
| **优先级** | P1 |

#### FE-USER-006: 搜索筛选

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-006 |
| **用例名称** | 按昵称搜索用户 |
| **前置条件** | sys_admin 已登录，存在"张三" |
| **操作步骤** | 1. 搜索框输入"张三" → 2. 点搜索 |
| **预期结果** | 只显示昵称包含"张三"的用户 |
| **测试目标** | 搜索筛选 |
| **优先级** | P2 |

#### FE-USER-007: 角色筛选

| 项目 | 内容 |
|------|------|
| **编号** | FE-USER-007 |
| **用例名称** | 按角色筛选用户 |
| **前置条件** | sys_admin 已登录 |
| **操作步骤** | 1. 角色下拉选 "HR专员" |
| **预期结果** | 只显示 userRole="hr" 的用户 |
| **测试目标** | 角色筛选 |
| **优先级** | P2 |

---

### 1.9 个人信息修改

#### FE-MY-001: 修改昵称

| 项目 | 内容 |
|------|------|
| **编号** | FE-MY-001 |
| **用例名称** | 修改个人昵称成功 |
| **前置条件** | 已登录 |
| **操作步骤** | 1. 用户头像下拉 → 个人信息 → 2. 修改昵称 → 3. 保存 |
| **预期结果** | 调 POST `/api/user/update/my` → Store 中 userInfo.userName 同步更新 → 顶部头像处昵称更新 |
| **测试目标** | 个人信息修改 |
| **优先级** | P1 |

#### FE-MY-002: 修改头像和简介

| 项目 | 内容 |
|------|------|
| **编号** | FE-MY-002 |
| **用例名称** | 修改头像和简介 |
| **前置条件** | 已登录 |
| **操作步骤** | 1. 个人信息 Modal → 2. 修改头像 URL → 3. 修改简介 → 4. 保存 |
| **预期结果** | Store 中 userAvatar 和 userProfile 同步更新 |
| **测试目标** | 个人信息修改 |
| **优先级** | P1 |

---

## 二、兼容性测试

| 编号 | 用例名称 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|--------|
| FE-CPT-LOGIN-001 | Chrome 90+ 登录 | Chrome 访问 `/login` 完成登录 | 所有交互正常，输入框/按钮样式正确 | P1 |
| FE-CPT-LOGIN-002 | Edge 90+ 登录 | Edge 访问登录页 | 正常 | P1 |
| FE-CPT-LOGIN-003 | Firefox 88+ 登录 | Firefox 访问登录页 | 正常 | P1 |
| FE-CPT-LOGIN-004 | Safari 14+ 登录 | Safari 访问登录页 | 居中卡片正常，输入框样式不偏移 | P2 |
| FE-CPT-LOGIN-005 | 移动端登录页 | 手机浏览器访问 `/login` | 卡片正常居中，字号可读，输入框不溢出 | P1 |
| FE-CPT-LOGIN-006 | 1366×768 分辨率 | 低分辨率查看登录页 | 卡片居中不超出视口 | P1 |
| FE-CPT-LOGIN-007 | Cookie 被禁用时提示 | 禁用 Cookie → 登录 | 提示"请启用 Cookie" 或登录失败后提示 | P2 |

---

## 三、性能测试

| 编号 | 用例名称 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|--------|
| FE-PF-LOGIN-001 | 登录响应时间 | 正常登录 | 含 API 请求 < 2 秒 | P1 |
| FE-PF-LOGIN-002 | 页面刷新恢复 | F5 刷新已登录页面 | 恢复用户信息 < 1 秒 | P2 |
| FE-PF-LOGIN-003 | 首次加载 | 清缓存首次访问 | 登录页首次渲染 < 1 秒 | P2 |

---

## 四、安全性测试

| 编号 | 用例名称 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|--------|
| FE-SEC-001 | 路由守卫拦截 | 未登录直接 URL 访问 `/system/user` | 跳转 `/login` | P0 |
| FE-SEC-002 | 角色路由拦截 | employee 直接访问 `/system/user` | 跳转 `/403` | P0 |
| FE-SEC-003 | 退出清理 Session | 退出 → 点浏览器后退 | 不回到已登录页面 | P1 |
| FE-SEC-004 | localStorage 无敏感数据 | F12 → Application → localStorage | 无 userPassword、userRole 权限码之外的敏感信息 | P1 |
| FE-SEC-005 | Cookie HttpOnly | F12 → Application → Cookies | JSESSIONID 标记为 HttpOnly（不可被 JS 读取） | P2 |
| FE-SEC-006 | XSS 输入过滤 | 在登录账号输入 `<script>alert(1)</script>` | 页面不弹窗（Vue 自动转义） | P2 |

---

## 五、用户体验测试

| 编号 | 用例名称 | 操作步骤 | 预期结果 | 优先级 |
|------|---------|---------|---------|--------|
| FE-UX-LOGIN-001 | 错误提示自动消失 | 出现错误 Alert 后等待 | 5 秒后自动消失或用户点击关闭 | P2 |
| FE-UX-LOGIN-002 | 登录页 Tab 顺序 | 按 Tab 键 | 焦点顺序：账号 → 密码 → 记住我 → 登录按钮 | P2 |
| FE-UX-LOGIN-003 | 注册成功 toast | 注册成功后 | toast 提示 + 自动跳转，不白屏 | P1 |
| FE-UX-LOGIN-004 | 退出确认 | 点击退出 | 直接退出（无需二次确认），跳转流畅 | P2 |
| FE-UX-LOGIN-005 | 登录页 Logo 展示 | 访问登录页 | HRMS Logo + "人力资源管理平台" 文字居中展示 | P2 |

---

## 六、自动化测试场景

| 编号 | 场景名称 | 涉及页面 | 自动化策略 | 优先级 |
|------|---------|---------|-----------|--------|
| FE-AUTO-001 | 登录全流程 | 登录页 + 首页 | Playwright/Cypress E2E | P0 |
| FE-AUTO-002 | 认证失败场景 | 登录页 | E2E（错误密码/空表单/网络错误） | P0 |
| FE-AUTO-003 | 注册全流程 | 注册页 | E2E | P1 |
| FE-AUTO-004 | 路由守卫拦截 | 各页面 | E2E（多角色） | P0 |
| FE-AUTO-005 | 退出登录 | 任意页 | E2E | P1 |
| FE-AUTO-006 | 菜单动态渲染 | 首页 | 组件级测试（多角色） | P1 |
| FE-AUTO-007 | 权限指令 | 含权限按钮的页面 | 组件级测试 | P1 |
| FE-AUTO-008 | 表单校验 | 登录页 + 注册页 | 组件级测试 | P1 |
| FE-AUTO-009 | Store 状态管理 | — | 单元测试 | P1 |
| FE-AUTO-010 | Axios 拦截器 | — | 单元测试（Mock 401/403） | P1 |

---

## 七、测试通过标准

1. 所有 **P0** 用例 100% 通过
2. **P1** 用例通过率 ≥ 98%
3. **P2** 用例通过率 ≥ 95%
4. 6 种角色（含 ban）登录后菜单和按钮权限正确
5. 路由守卫正确拦截未登录用户和无权限用户
6. 退出登录后状态完全清除，无法回到已登录页面
7. Session 过期后 Axios 拦截器正确处理 401
8. 页面刷新后登录态正常恢复
9. 移动端登录页适配正常
